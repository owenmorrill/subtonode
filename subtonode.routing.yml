subtonode:
  path: '/admin/structure/webform/manage/{webform}/submission/{sid}/subtonode'
  defaults:
    _controller: '\Drupal\subtonode\Controller\SubToNodeController::subtonode'
    _title: 'Create Node'
  requirements:
    _permission: 'access content'
